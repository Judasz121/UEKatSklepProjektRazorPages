@page
@using BasiaProjektRazorPages.DbModels
@model BasiaProjektRazorPages.Pages.AdminPanel.Delivery.ListModel
@{
}
@if (!AccountHelper.loggedInVerified || !AccountHelper.loggedInAccount.JestAdminem )
{
    <div class="text-center">
        <h3>Nie mosz uprawnień do tej strony.</h3>
    </div>
}
else
{
    <div class="container admin-panel-settings bg-white text-ce" style="width:min(36rem)">
        <div>
            <h3 style="border-bottom:1px solid black;text-align:center;">Admin Panel: @AccountHelper.loggedInAccount.LoginUzytkownika</h3>
            <div class="admin-button-container">
                <a class="button-animation" style="padding:0.5rem 1.5rem" asp-page="/AdminPanel/Index">
                    Strona główna
                    <div class="btn_horizontal"></div>
                    <div class="btn_vertical"></div>
                </a>
            </div>
        </div>
    </div>
    <br />
    <div class="container admin-accounts-container text-center" style="width: min(75rem) !important;background-color:white;color:black;">
        <table id="accounts-table">
            <thead>
                <tr>
                    <th>
                        @{
                            string displayName = null;
                            displayName = @Html.DisplayNameFor(model => model.delivery.FirstOrDefault().ID_Dostawy);
                        }
                        <input class="form-control" type="text" name="@displayName" placeholder="@displayName" />
                    </th>
                    <th>
                        @{
                            displayName = @Html.DisplayNameFor(model => model.delivery.FirstOrDefault().ID_Dostawcy);
                        }
                        <input class="form-control" type="text" name="@displayName" placeholder="@displayName" />
                    </th>
                    <th>
                        @{
                            displayName = @Html.DisplayNameFor(model => model.delivery.FirstOrDefault().ID_Magazynu);
                        }
                        <input class="form-control" type="text" name="@displayName" placeholder="@displayName" />
                    </th>
                    <th>
                        @{
                            displayName = @Html.DisplayNameFor(model => model.delivery.FirstOrDefault().ID_Produkt_magazyn);
                        }
                        <input class="form-control" type="text" name="@displayName" placeholder="@displayName" />
                    </th>
                    <th>
                        @{
                            displayName = @Html.DisplayNameFor(model => model.delivery.FirstOrDefault().Ilosc_produktu);
                        }
                        <input class="form-control" type="text" name="@displayName" placeholder="@displayName" />
                    </th>
                    <th style="width:min(9rem)">
                        Opcje
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (Dostawa dev in Model.delivery)
                {
                    <tr>
                        <td>
                            @Html.DisplayFor(model => dev.ID_Dostawy)
                        </td>
                        <td>
                            @Html.DisplayFor(model => dev.ID_Dostawcy)
                        </td>
                        <td>
                            @Html.DisplayFor(model => dev.ID_Magazynu)
                        </td>
                        <td>
                            @Html.DisplayFor(model => dev.ID_Produkt_magazyn)
                        </td>
                        <td>
                            @Html.DisplayFor(model => dev.Ilosc_produktu)
                        </td>
                        <td>
                            <div id="options">
                                <a class="btn btn-primary" asp-page="/AdminPanel/Delivery/View" asp-route-id="@dev.ID_Dostawy">
                                    <i class="far fa-eye"></i>
                                </a>
                                <a class="btn btn-warning" asp-page="/AdminPanel/Delivery/Edit" asp-route-id="@dev.ID_Dostawy">
                                    <i class="far fa-edit"></i>
                                </a>
                                <a class="btn btn-danger" asp-page="/AdminPanel/Delivery/Delete" asp-route-id="@dev.ID_Dostawy">
                                   <i class="fa-solid fa-trash"></i>
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>

    </div>
    <script>
        document.addEventListener('DOMContentLoaded', e => {
            GenerateDataTables($("#accounts-table"));
        });
    </script>
}